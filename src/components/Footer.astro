---
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="page footer-grid">
    <div class="footer-brand">
      <p class="footer-brand__name">Julia Tang</p>
      <p class="t-caption">© {year}</p>
    </div>

    <nav class="footer-nav" aria-label="Footer">
      <div class="footer-col">
        <ul>
          <li><a href={`${base}/`}>Work</a></li>
          <li><a href={`${base}/about`}>About</a></li>
          <li><a href={`${base}/resume.pdf`} target="_blank" rel="noopener">Resume</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <ul>
          <li><a href="mailto:hello@juliatang.design">Email</a></li>
          <li><a href="https://www.linkedin.com/in/juliaxtang/" target="_blank" rel="noopener">LinkedIn</a></li>
        </ul>
      </div>
    </nav>
  </div>
</footer>

<style>
  .site-footer {
    padding: var(--s-12) 0;
    border-top: 1px solid var(--border-subtle);
    background: var(--bg-canvas);
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 1.2fr 2fr;
    gap: var(--s-12);
    align-items: start;
  }
  /* Match the header brand: display serif, no italic. */
  .footer-brand__name {
    margin: 0;
    font-family: var(--font-display);
    font-size: 22px;
    letter-spacing: -0.01em;
    color: var(--text-primary);
  }
  .footer-brand .t-caption { margin-top: var(--s-2); }

  .footer-nav {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--s-8);
    align-items: start;
  }
  .footer-col ul { list-style: none; margin: 0; padding: 0; display: grid; gap: var(--s-2); }
  .footer-col a {
    color: var(--text-secondary);
    transition: color var(--dur-fast) var(--ease);
    font-size: 15px;
  }
  .footer-col a:hover { color: var(--text-primary); }

  @media (max-width: 720px) {
    .footer-grid { grid-template-columns: 1fr; gap: var(--s-8); }
    .footer-nav { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  }
</style>
